---
const { class: className, id } = Astro.props
const { children } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container lg:max-w-xl md:max-w-2xl`}
>
  <slot />
</section>